import { fakematch, fakemodel, faketeam, fakeuser } from "../types/fake-data"; // or your custom output path
console.log(fakematch());
console.log(fakemodel());
export async function insertMockData() {
  return Promise.resolve();
}
